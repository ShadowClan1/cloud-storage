# Popsmokemedia-backend

# changes 

admin/set-password -> now token is taken into account through params not from body

admin/set-password -> now token is taken into account through params not from body


admin/set-password -> now token is taken into account through params not from body




